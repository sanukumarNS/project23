import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  postParent: {
    margin: 20,
    padding: 20,
  },
  postTitle: {
    fontSize: 20,
  },
  postBody: {
    fontSize: 14,
  },
});

export default styles;
