export const BASE_URL = 'https://jsonplaceholder.typicode.com';

// Endpoint
export const TODOS = (pageNumber: number) => '/todos/' + pageNumber;
export const POSTS = '/posts';
export const ALBUMS = '/albums';
